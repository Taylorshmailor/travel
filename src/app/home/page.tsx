'use client';
import { Button, Card, TextField, FormControlLabel, Checkbox } from "@mui/material";
import { styled } from "@mui/system";

const PageWrapper = styled('div')({
  height: '100vh',
  width: '100%',
  backgroundColor: '#F6F4EE',
  padding: '2%'
})

const Home = () => {

  return (
    <PageWrapper>Placeholder Home Page</PageWrapper>
  )
};

export default Home;