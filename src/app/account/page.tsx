'use client';
import { Button, styled } from "@mui/material";

const PageWrapper = styled('div')({
  height: '100vh',
  width: '100%',
  backgroundColor: '#F6F4EE',
  padding: '2%'
})

const Account = () => {

  return (
    <PageWrapper>
      <div>Placeholder Account Page</div>
    </PageWrapper>
  )
};

export default Account;